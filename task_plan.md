# Task Plan: 章节新增/改名父节点外键修复

## Goal
修复章节新增或改名时 `parent_id` 指向已不存在章节导致 Supabase 外键报错的问题。

## Phases
- [x] Phase 1: 定位新增、改名、删除和保存接口链路
- [x] Phase 2: 前端保存前清理无效父节点
- [x] Phase 3: 后端 upsert 前校验父章节仍属于当前项目
- [x] Phase 4: 后端编译和前端构建验证

## Decisions Made
- 前端层级归一化时，孤儿章节不再从目录树消失，而是提升为一级章节。
- 新增、保存、改名、排序时只提交本地仍存在且格式合法的父章节 id。
- 后端作为最终保护：如果 `parent_id` 在当前项目中不存在，则自动置空，避免数据库外键错误暴露成 500。

## Status
**Complete** - 章节父节点外键修复完成，后端编译和前端构建通过。
